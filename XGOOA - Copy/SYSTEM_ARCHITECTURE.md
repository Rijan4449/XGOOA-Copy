# System Architecture

Visual guide to the XGOOA Invasive Species Risk Prediction System.

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                         (Web Browser)                           │
├─────────────────────────────────────────────────────────────────┤
│  Overview  │  Risk Map  │  Interpretation  │  Risk Scores      │
│  Tab       │  Tab       │  Tab             │  Tab              │
└──────┬──────────┬──────────────┬────────────────┬───────────────┘
       │          │              │                │
       │ HTTP     │ HTTP         │ HTTP           │ HTTP
       │ GET      │ POST         │ GET            │ POST
       │          │              │                │
┌──────▼──────────▼──────────────▼────────────────▼───────────────┐
│                      FLASK REST API                              │
│                    (flask_api.py)                                │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌──────────┐ │
│  │  /health   │  │ /predict   │  │ /feature-  │  │ /risk-   │ │
│  │            │  │            │  │ importance │  │ scores   │ │
│  └────────────┘  └────────────┘  └────────────┘  └──────────┘ │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           │ Function Calls
                           │
┌────────────────���─────────▼───────────────────────────────────────┐
│                    BACKEND MODULE                                │
│                    (backend.py)                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  get_risk_predictions()                                   │  │
│  │  get_most_contributing_feature()                          │  │
│  │  get_feature_importance_analysis()                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────┬────────────────┬────────────────┬─────────────────────┘
           │                │                │
           │ Load           │ Load           │ Load
           │                │                │
┌───────���──▼────────┐  ┌───▼──────────┐  ┌──▼──────────────────┐
│  XGBoost Model    │  │ Preprocessor │  │  Species Dataset    │
│  (JSON)           │  │ (Joblib)     │  │  (CSV)              │
│                   │  │              │  │                     │
│ mooa_xgb_model_   │  │ mooa_pre-    │  │ super_dataset.csv   │
│ v4.json           │  │ processor_   │  │                     │
│                   │  │ v4.joblib    │  │ - Species traits    │
│ - Trained model   │  │              │  │ - Lake data         │
│ - Feature         │  │ - Transforms │  │ - Presence records  │
│   importances     │  │   input data │  │                     │
└───────────────────┘  └──────────────┘  └─────────────────────┘
```

## 🔄 Data Flow: Risk Prediction

```
1. USER INPUT
   ┌─────────────────────────────────────┐
   │ Species: Anabas testudineus         │
   │ Temperature: 27.0°C                 │
   │ pH: 7.5                             │
   │ Salinity: 0.5 ppt                   │
   │ Dissolved Oxygen: 6.0 mg/L          │
   │ BOD: 2.0 mg/L                       │
   │ Turbidity: 10.0 NTU                 │
   └─────────────────┬───────────────────┘
                     │
                     ▼
2. FRONTEND (JavaScript)
   ┌─────────────────────────────────────┐
   │ Collect form values                 │
   │ Build JSON payload                  │
   │ POST to /api/predict                │
   └─────────────────┬───────────────────┘
                     │
                     ▼
3. FLASK API
   ┌─────────────────────────────────────┐
   │ Validate input                      │
   │ Call backend.get_risk_predictions() │
   └─────────────────┬───────────────────┘
                     │
                     ▼
4. BACKEND MODULE
   ┌─────────────────────────────────────┐
   │ A. Fetch species data from dataset  │
   │    ├─ Biological traits             │
   │    └─ Temperature preferences       │
   │                                     │
   │ B. Build input for 13 lakes         │
   │    ├─ Combine species + lake data   │
   │    └─ Add user input parameters     │
   │                                     │
   │ C. Engineer derived features        │
   │    ├─ temp_pref_range               │
   │    ├─ ph_difference                 │
   │    └─ temp_in_pref_range            │
   │                                     │
   │ D. Transform data                   │
   │    └─ Use preprocessor pipeline     │
   │                                     │
   │ E. Generate predictions             │
   │    └─ XGBoost model.predict_proba() │
   │                                     │
   │ F. Calculate similarity             │
   │    ├─ 6D Euclidean distance         │
   │    └─ Exponential decay             │
   │                                     │
   │ G. Adjust scores                    │
   │    └─ adjusted = raw × similarity   │
   │                                     │
   │ H. Categorize risk                  │
   │    ├─ Low: < 0.34                   │
   │    ├─ Medium: 0.34-0.67             │
   │    └─ High: ≥ 0.67                  │
   │                                     │
   │ I. Check presence                   │
   │    └─ Query dataset for records     │
   └─────────────────┬───────────────────┘
                     │
                     ▼
5. RETURN PREDICTIONS
   ┌─────────────────────────────────────┐
   │ {                                   │
   │   "predictions": [                  │
   │     {                               │
   │       "lake_name": "Laguna_de_Bay", │
   │       "raw_score": 0.67,            │
   │       "adjusted_score": 0.45,       │
   │       "risk_level": "Medium",       │
   │       "similarity": 0.67,           │
   │       "presence": "Yes"             │
   │     },                              │
   │     ... 12 more lakes               │
   │   ]                                 │
   │ }                                   │
   └─────────────────┬───────────────────┘
                     │
                     ▼
6. FRONTEND UPDATE
   ┌─────────────────────────────────────┐
   │ ├─ Update map markers               │
   │ ├─ Color by risk level              │
   │ ├─ Add popups with details          │
   │ ├─ Update risk scores table         │
   │ └─ Show warning if needed           │
   └─────────────────────────────────────┘
```

## 🧮 Risk Calculation Algorithm

```
┌─────────────────────────────────────────────────────────────┐
│                    RISK CALCULATION                         │
└─────────────────────────────────────────────────────────────┘

Step 1: RAW PREDICTION
┌──────────────────────────────────────┐
│ Input: Species + Lake + User Params │
│        ↓                             │
│ XGBoost Model                        │
│        ↓                             │
│ Raw Probability: 0.67                │
└──────────────────────────────────────┘

Step 2: ENVIRONMENTAL SIMILARITY
┌──────────────────────────────────────┐
│ User Input:                          │
│   [pH=7.5, Sal=0.5, DO=6.0,          │
│    BOD=2.0, Turb=10.0, Temp=27.0]    │
│                                      │
│ Lake Baseline:                       │
│   [pH=9.12, Sal=0.746, DO=7.54,      │
│    BOD=1.93, Turb=161.88, Temp=28.5] │
│        ↓                             │
│ Distance = √Σ(user[i] - lake[i])²   │
│          = 152.3                     │
│        ↓                             │
│ Similarity = exp(-distance / 10)     │
│            = exp(-152.3 / 10)        │
│            = 0.67                    │
└──────────────────────────────────────┘

Step 3: ADJUSTED SCORE
┌──────────────────────────────────────┐
│ Adjusted Score = Raw × Similarity    │
│                = 0.67 × 0.67          │
│                = 0.45                 │
└───────────���──────────────────────────┘

Step 4: RISK CATEGORIZATION
┌──────────────────────────────────────┐
│ if score < 0.34:                     │
│     risk = "Low" 🟢                  │
│ elif score < 0.67:                   │
│     risk = "Medium" 🟡               │
│ else:                                │
│     risk = "High" 🔴                 │
│                                      │
│ Result: 0.45 → "Medium" 🟡           │
└──────────────────────────────────────┘
```

## 📊 Feature Importance Aggregation

```
┌─────────────────────────────────────────────────────────────┐
│              FEATURE IMPORTANCE ANALYSIS                    │
└─────────────────────────────────────────────────────────────┘

Step 1: EXTRACT FROM MODEL
┌───────────────────────────────────���──┐
│ XGBoost Model                        │
���   ↓                                  │
│ booster.get_score(type='gain')       │
│   ↓                                  │
│ {                                    │
│   "f0": 245.6,                       │
│   "f1": 198.3,                       │
│   "f2": 187.2,                       │
│   ...                                │
│ }                                    │
└──────────────────────────────────────┘

Step 2: MAP TO FEATURE NAMES
┌──────────────────────────────────────┐
│ Preprocessor                         │
│   ↓                                  │
│ get_feature_names_out()              │
│   ↓                                  │
│ {                                    │
│   "f0": "wb_bod_min",                │
│   "f1": "input_bod",                 │
│   "f2": "wb_bod_max",                │
│   ...                                │
│ }                                    │
└──────────���───────────────────────────┘

Step 3: AGGREGATE BY PARAMETER
┌──────────────────────────────────────┐
│ Keyword Matching:                    │
│                                      │
│ BOD Features:                        │
│   wb_bod_min: 245.6                  │
│   input_bod: 198.3                   │
│   wb_bod_max: 187.2                  │
│   bod_difference: 156.8              │
│   ... (8 features)                   │
│   ─────────────────                  │
│   Total: 1247.3                      │
│                                      │
│ Temperature Features:                │
│   temp_pref_min: 203.4               │
│   input_temp: 189.7                  │
│   ... (12 features)                  │
│   ─────────────────                  │
│   Total: 1103.5                      │
│                                      │
│ ... (4 more parameters)              │
└──────────────────────────────────────┘

Step 4: SORT AND RETURN
┌───────────────��──────────────────────┐
│ [                                    │
│   {                                  │
│     "parameter": "BOD",              │
│     "total_importance": 1247.3,      │
│     "percentage": 23.4%              │
│   },                                 │
│   {                                  │
│     "parameter": "Temperature",      │
│     "total_importance": 1103.5,      │
│     "percentage": 20.7%              │
│   },                                 │
│   ...                                │
│ ]                                    │
└──────────────────────────────────────┘
```

## 🗺️ Frontend-Backend Mapping

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND PAGES                           │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  Overview.html   │
├──────────────────┤
│ Displays:        │
│ - Most           │
│   Contributing   │
│   Feature        │
│                  │
│ Backend:         │
│ GET /api/        │
│ overview/most-   │
│ contributing     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  predict.html    │
├──────────────────┤
│ Displays:        │
│ - Interactive    │
│   Leaflet map    │
│ - Risk markers   │
│ - Popups         │
│                  │
│ Backend:         │
│ POST /api/       │
│ geojson          │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│interpretation.   │
│html              │
├──────────────────┤
│ Displays:        │
│ - Bar chart      │
│ - Feature        │
│   breakdown      │
│                  │
│ Backend:         │
│ GET /api/        │
│ interpretation/  │
│ feature-         │
│ importance       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ risk_scores.html │
├──────────────────┤
│ Displays:        │
│ - Table with     │
│   Lake, Region,  │
│   Score, Risk,   │
│   Presence       │
│                  │
│ Backend:         │
│ POST /api/       │
│ risk-scores      │
└──────────────────┘
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
└─────────────────────────────────────────────────────────────┘

Layer 1: INPUT VALIDATION
┌──────────────────────────────────────┐
│ Flask API                            │
│ ├─ Check required fields             │
│ ├─ Validate data types               │
│ ├─ Range checks                      │
│ └─ Sanitize inputs                   │
└──────────────────────────────────────┘

Layer 2: ERROR HANDLING
┌──────────────────────────────────────┐
│ Backend Module                       │
│ ├─ Try-catch blocks                  │
│ ├─ Graceful degradation              │
│ ├─ No stack traces to client         │
│ └─ Logged errors                     │
└──────────────────────────────────────┘

Layer 3: CORS POLICY
┌──────────────────────────────────────┐
│ Flask CORS                           │
│ ├─ Whitelist origins                 │
│ ├─ Allowed methods                   │
│ └─ Credentials handling              │
└──────────────────────────────────────┘

Layer 4: HTTPS (Production)
┌──────────────────────────────────────┐
│ Nginx / Load Balancer                │
│ ├─ SSL/TLS encryption                │
│ ├��� Certificate management            │
│ └─ Secure headers                    │
└──────────────────────────────────────┘

Layer 5: RATE LIMITING (Future)
┌──────────────────────────────────────┐
│ API Gateway                          │
│ ├─ Request throttling                │
│ ├─ IP-based limits                   │
│ └─ API key management                │
└──────────────────────────────────────┘
```

## 📦 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  PRODUCTION DEPLOYMENT                      │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────┐
│ Load Balancer / Nginx                │
│ ├─ SSL Termination                   │
│ ├─ Static file serving               │
│ └─ Reverse proxy                     │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│ Gunicorn (WSGI Server)               │
│ ├─ Multiple workers                  │
│ ├─ Process management                │
│ └─ Load distribution                 │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│ Flask Application                    │
│ ├─ API endpoints                     │
│ ├─ Request handling                  │
│ └─ Response formatting               │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│ Backend Module                       │
│ ├─ Business logic                    │
│ ├─ Model inference                   │
│ └─ Data processing                   │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│ File System                          │
│ ├─ Model files                       │
│ ├─ Dataset                           │
│ └─ Logs                              │
└──────────────────────────────────────┘
```

## 🔄 Request-Response Cycle

```
Time: 0ms
┌─────────────────────────────────────┐
│ User clicks "Run Prediction"        │
└─────────────────┬───────────────────┘
                  │
Time: 10ms        ▼
┌─────────────────────────────────────┐
│ JavaScript collects form data       │
│ Builds JSON payload                 │
└────────────���────┬───────────────────┘
                  │
Time: 20ms        ▼
┌─────────────────────────────────────┐
│ HTTP POST to /api/predict           │
│ Network latency                     │
└─────────────────┬───────────────────┘
                  │
Time: 30ms        ▼
┌─────────────────────────────────────┐
│ Flask receives request              │
│ Validates input                     │
└─────────────────┬───────────────────┘
                  │
Time: 35ms        ▼
┌─────────────────────────────────────┐
│ Backend.get_risk_predictions()      │
│ ├─ Fetch species (5ms)              │
│ ├─ Build input (10ms)               │
│ ├─ Transform (20ms)                 │
│ ├─ Predict (30ms)                   │
│ ├─ Calculate similarity (5ms)       │
│ └─ Format results (10ms)            │
└─────────────────┬��──────────────────┘
                  │
Time: 115ms       ▼
┌─────────────────────────────────────┐
│ Flask formats JSON response         │
└─────────────────┬───────────────────┘
                  │
Time: 125ms       ▼
┌─────────────────────────────────────┐
│ HTTP response sent                  │
│ Network latency                     │
└─────────────────┬───────────────────┘
                  │
Time: 135ms       ▼
┌─────────────────────────────────────┐
│ JavaScript receives response        │
│ Updates UI:                         │
│ ├─ Map markers (20ms)               │
│ ├─ Risk table (10ms)                │
│ └─ Animations (30ms)                │
└─────────────────────────────────────┘

Total Time: ~195ms
```

## 📊 Data Model

```
┌───────────────��─────────────────────────────────────────────┐
│                      DATA ENTITIES                          │
└─────────────────────────────────────────────────────────────┘

Species
├─ species (str)
├─ common_name (str)
├─ family (str)
├─ order (str)
├─ status (str)
├─ feeding_type (str)
├─ temp_pref_min (float)
├─ temp_pref_max (float)
├─ length_max (float)
├─ weight_max (float)
└─ trophic_lvl (float)

Lake
├─ name (str)
├─ region (str)
├─ latitude (float)
├─ longitude (float)
├─ pH (float)
├─ salinity_ppt (float)
├─ dissolved_oxygen_mgL (float)
├─ bod_mgL (float)
├─ turbidity_ntu (float)
└─ temperature_c (float)

Prediction
├─ lake_name (str)
├─ region (str)
├─ latitude (float)
├─ longitude (float)
├─ raw_score (float)
├─ adjusted_score (float)
├─ risk_level (str)
├─ similarity (float)
└─ presence (str)

FeatureImportance
├─ parameter (str)
├─ total_importance (float)
├─ feature_count (int)
└─ percentage (float)
```

This architecture provides a scalable, maintainable, and secure system for invasive species risk prediction! 🚀
